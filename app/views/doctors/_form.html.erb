<%= form_with(model: doctor, local: true) do |form| %>
  <% if doctor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(doctor.errors.count, "error") %> prohibited this doctor from being saved:</h2>

      <ul>
      <% doctor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :first_name %>
    <%= form.text_field :first_name, id: :doctor_first_name %>
  </div>

  <div class="field">
    <%= form.label :time %>
		<%= form.select  :time, options_for_select([["09.00-10.00", 1], ["10.00-11.00", 2], ["11.00-12.00", 3],["13.00-14.00", 4], ["14.00-15.00",5],["15.00-16.00", 6]]),{}, id: :doctor_time %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
